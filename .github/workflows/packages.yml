name: Packages

on:
  pull_request:
  workflow_dispatch:
# PR gate: build the combined daemon+dashboard package (amd64) and exercise the full
# install -> verify -> remove/purge lifecycle. Multi-arch builds + the rolling release
# run in release.yml on pushes to main.

permissions:
  contents: read

jobs:
  deb:
    name: Build and smoke-test .deb (Debian/Ubuntu)
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake git dpkg-dev fakeroot

      - name: Build the combined .deb
        run: ./scripts/package.sh

      - name: Inspect package
        run: |
          set -euo pipefail
          deb=$(ls build-pkg/easy-failover_*_amd64.deb)
          dpkg-deb -I "$deb"
          c=$(dpkg-deb -c "$deb")
          for f in /usr/bin/easy-failover \
                   /usr/lib/systemd/system/easy-failover.service \
                   /usr/lib/systemd/system/easy-failover-dashboard.service \
                   /usr/lib/easy-failover-dashboard/server.js \
                   /etc/easy-failover/config.example.toml \
                   /etc/easy-failover-dashboard/dashboard.env.example; do
            echo "$c" | grep -qE "${f}\$" || { echo "::error::package missing $f"; exit 1; }
          done
          if echo "$c" | grep -E '/etc/(init\.d|sv|dinit\.d|s6)/'; then
            echo "::error::package unexpectedly contains alternative init-system files"; exit 1
          fi

      - name: Install (resolves iproute2 / iputils-arping / nodejs)
        run: |
          set -euo pipefail
          deb=$(ls build-pkg/easy-failover_*_amd64.deb)
          sudo apt-get install -y "./$deb"

      - name: Verify install (daemon + dashboard + auto-started dashboard service)
        run: |
          set -euo pipefail
          test -x /usr/bin/easy-failover
          test -f /etc/easy-failover/config.toml                       # seeded by postinst
          grep -qE '^\s*enabled\s*=\s*true' /etc/easy-failover/config.toml   # API on by default
          test -f /usr/lib/systemd/system/easy-failover.service
          test -f /usr/lib/easy-failover-dashboard/server.js
          test -f /etc/easy-failover-dashboard/dashboard.env           # seeded by postinst
          getent passwd easy-failover-dashboard                        # dashboard user created
          easy-failover --config /etc/easy-failover/config.toml --validate-config
          # The dashboard service is auto-enabled+started by postinst; the daemon is not.
          systemctl is-active --quiet easy-failover-dashboard.service \
            || { echo "::error::dashboard service not active"; journalctl -u easy-failover-dashboard --no-pager | tail -40; exit 1; }
          ok=0
          for _ in $(seq 1 20); do
            if curl -fsS -o /dev/null http://127.0.0.1:3000/; then ok=1; break; fi
            sleep 1
          done
          test "$ok" = "1" || { echo "::error::dashboard did not serve a request"; exit 1; }
          ! systemctl is-active --quiet easy-failover.service || echo "note: daemon active (not expected, but harmless)"

      - name: Remove keeps config; purge removes everything
        run: |
          set -euo pipefail
          sudo apt-get remove -y easy-failover
          test ! -e /usr/bin/easy-failover
          test -f /etc/easy-failover/config.toml || { echo "::error::remove should keep config"; exit 1; }
          sudo apt-get purge -y easy-failover
          test ! -e /etc/easy-failover || { echo "::error::purge should remove /etc/easy-failover"; exit 1; }
          test ! -e /etc/easy-failover-dashboard || { echo "::error::purge should remove dashboard config"; exit 1; }
          ! getent passwd easy-failover-dashboard || { echo "::error::purge should remove the dashboard user"; exit 1; }

      - name: Upload .deb artifact
        uses: actions/upload-artifact@v6
        with:
          name: easy-failover-deb
          path: build-pkg/easy-failover_*_amd64.deb
          if-no-files-found: error

  rpm:
    name: Build and smoke-test .rpm (Fedora)
    runs-on: ubuntu-latest
    container:
      image: fedora:latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install build tools
        run: dnf install -y cmake gcc-c++ make rpm-build git nodejs npm

      - name: Build the combined .rpm
        run: ./scripts/package.sh

      - name: Inspect package
        run: |
          set -euo pipefail
          rpm_file=$(ls build-pkg/easy-failover-*.rpm)
          name=$(rpm -qp --queryformat '%{NAME}' "$rpm_file")
          test "$name" = "easy-failover" || { echo "::error::unexpected RPM name: $name"; exit 1; }
          rpm -qpl "$rpm_file" | grep -qE '/usr/lib/easy-failover-dashboard/server\.js$'
          rpm -qpl "$rpm_file" | grep -qE '/usr/lib/systemd/system/easy-failover-dashboard\.service$'

      - name: Install (resolves iproute / iputils / nodejs)
        run: |
          set -euo pipefail
          rpm_file=$(ls build-pkg/easy-failover-*.rpm)
          dnf install -y "./$rpm_file"

      - name: Verify install
        run: |
          set -euo pipefail
          test -x /usr/bin/easy-failover
          test -f /etc/easy-failover/config.toml
          test -f /usr/lib/easy-failover-dashboard/server.js
          getent passwd easy-failover-dashboard
          easy-failover --config /etc/easy-failover/config.toml --validate-config
          # systemd isn't running in the container, so the service isn't started here; the
          # standalone server itself is smoke-tested directly.
          node_bin=$(command -v node || command -v nodejs)
          runuser -u easy-failover-dashboard -- env PORT=3000 HOSTNAME=127.0.0.1 \
            "$node_bin" /usr/lib/easy-failover-dashboard/server.js &
          ok=0
          for _ in $(seq 1 20); do
            if curl -fsS -o /dev/null http://127.0.0.1:3000/; then ok=1; break; fi
            sleep 1
          done
          test "$ok" = "1" || { echo "::error::dashboard did not serve a request"; exit 1; }

      - name: Erase removes config and user
        run: |
          set -euo pipefail
          dnf remove -y easy-failover
          test ! -e /usr/bin/easy-failover
          test ! -e /etc/easy-failover || { echo "::error::erase should remove /etc/easy-failover"; exit 1; }
          test ! -e /etc/easy-failover-dashboard || { echo "::error::erase should remove dashboard config"; exit 1; }

      - name: Upload .rpm artifact
        uses: actions/upload-artifact@v6
        with:
          name: easy-failover-rpm
          path: build-pkg/easy-failover-*.rpm
          if-no-files-found: error
